<style>
    .pagination {
    /* CSS 변수 값을 여기서 재정의(override)합니다. */
        --bs-pagination-color: #5928B1;                     /* 기본 페이지 번호 글자색 */
        --bs-pagination-hover-color: #5928B1;               /* 마우스 올렸을 때 글자색 */
        --bs-pagination-active-bg: #5928B1;                  /* 현재 페이지 배경색 */
        --bs-pagination-active-border-color: #5928B1;        /* 현재 페이지 테두리 색 */
        --bs-pagination-disabled-color: #aeaeae;             /* 비활성화된 버튼 글자색 */
        --bs-pagination-border-radius: .7rem;
    }
    /* 현재 페이지(active)의 글자색은 가독성을 위해 보통 흰색으로 유지합니다. */
    .page-item.active .page-link {
        color: #fff;
    }
    .e_txt {
        display: block; /* h5는 기본적으로 block이지만, 명시적으로 선언하면 좋습니다. */
        width: 100%;    /* 부모 요소의 너비를 꽉 채웁니다. */
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
    .card-text {
        font-size: 0.9rem;
    }
</style>

