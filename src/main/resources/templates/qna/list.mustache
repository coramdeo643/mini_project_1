{{> layout/header}}

<div class="container p-5">
    <div style="margin:1rem 0" class="d-grid gap-2 d-md-flex justify-content-md-end">
        <a href="/qna/save-form" class="btn btn-primary">글쓰기</a>
    </div>

    {{#qnaList}}
        <div class="card mb-3">
            <div class="card-body">
                <!-- {{! title}} 은 현재 Board 객체읭 title 멤버 변수에 값을 출력한다.
                        머스태치는 자동으로 getTitle()메서드를 호출한 것이다.-->
                <h4 class="card-title mb-3">{{title}}</h4>
                <p> 작성자 : {{user.username}}&nbsp|&nbsp{{time}}</p>
                <a href="/qna/{{id}}" class="btn btn-primary">상세보기</a>
            </div>
        </div>
    {{/qnaList}}

    <ul class="pagination d-flex justify-content-center">
        <li class="page-item disabled"><a class="page-link" href="#">Previous</a></li>
        <li class="page-item"><a class="page-link" href="#">Next</a></li>
    </ul>

</div>

{{> layout/footer}}